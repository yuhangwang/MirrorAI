"""
Given a series of 3D points, return
many reflections (mirror images)
"""
from numpy import ndarray
from typing import Tuple


def simple_reflections(points, n, size):
    # type: (ndarray, int) -> ndarray
    """Compute reflections

    Args:
        points (ndarray): a Nx3 matrix which
            represents N 3D spatial points
        n (int): number of reflections to generate
        size (tuple): a 2-tuple representing
            output image sizes
    Returns:
        return list of 2D images of user defined size
    """
    return []
